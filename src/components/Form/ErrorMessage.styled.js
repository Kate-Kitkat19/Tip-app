import styled from 'styled-components';

export const ErrorStyled = styled.p`
  margin: 0;
  font-family: inherit;
  font-weight: 700;
  font-size: 16px;
  line-height: 1.5;
  position: absolute;
  top: 0;
  right: 0;

  color: #e17457;
`;
